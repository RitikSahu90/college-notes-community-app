import React from 'react';

const SearchBar = () => {
  return (
    <input
      type="text"
      className="form-control mb-3"
      placeholder="Search notes..."
    />
  );
};

export default SearchBar;
